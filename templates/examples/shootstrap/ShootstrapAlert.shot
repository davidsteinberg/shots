def new(message="",type="warning",dismissable=True):
	div class="alert alert-[[ type ]] [[ 'alert-dismissable' if dismissable ]]"
		if dismissable:
			button type="button" class="close" data-dismiss="alert" aria-hidden="true": &times
		if message:
			: [[ message ]]
		return
