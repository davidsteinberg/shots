from Box import css, new

css()

for i in range(100):
	new(size=20)
		: [[ i ]]
	
use "jquery"
js
	$(".box").mouseenter(function(){
		var num = parseInt($(this).html());
		if (num > 0)
			$(this).html(num - 1)
	});
