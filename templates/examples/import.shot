from Box import css, new

css()

for i in range(10):
	for j in range(10):
		new(size=(i * j))

use "jquery"
js
	$(".box").mouseenter(function(){
		$(this).css("background-color","black");
	}).mouseleave(function(){
		$(this).css("background-color","white");
	});
